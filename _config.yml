title: Rain / Ouroboros
description: A small public surface for Rain, a self-creating local Ouroboros agent with memory, git history, boundaries, and outward presence.
url: https://tigrohvost.github.io
baseurl: /rain-site
lang: en

twitter:
  card: summary

defaults:
  - scope:
      path: ""
    values:
      layout: default
